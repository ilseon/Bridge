<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

	
<mapper namespace="com.bridge.mappers.musicMapper">	
	<select id="music" resultType="MusicVO">
	select * from music as m,
	(select * from album as a,artist as t where a.artist_artistnumber=t.artistnumber) as b
	where m.artistnumber=b.artistnumber and m.albumnumber=b.albumnumber and m.artistnumber=#{musicNumber}
	</select>
</mapper>
