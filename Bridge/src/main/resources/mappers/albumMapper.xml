<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bridge.mappers.albumMapper">	
	<select id="test" resultType="com.bridge.app.domain.UserVO">
		select * from user
	</select>	
	<insert id="regist">
		insert into album(albumname, albumtype, albumdate, albumgenre, artist_artistnumber, albumimg, albumcontent)
		values(#{albumName}, #{albumType}, #{albumDate}, #{albumGenre}, #{artistNumber}, #{albumImg}, #{albumContent})
	</insert>	
</mapper>