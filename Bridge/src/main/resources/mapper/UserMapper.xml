<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.woojun.mappers.UserMapper">
	<insert id="create"><!-- 파라미터 생략하고 작성 -->
		insert into bridgedb.user(username, userbirthday, userid, userpassword, userphone)
		values(#{username}, #{userbirthday}, #{userid}, #{userpassword}, #{userphone})
	</insert>
	
	</mapper>
	
	
	
